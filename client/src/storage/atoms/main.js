import { atom } from "recoil";

export const agentsState = atom({
  key: "agentsState",
  default: [],
});
export const mapsState = atom({
  key: "mapsState",
  default: [],
});
export const weaponsState = atom({
  key: "weaponsState",
  default: [],
});
export const skinsState = atom({
  key: "skinsState",
  default: [],
});
